{{- if .Values.app.secretConfig }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range .Values.app.secretConfig }}
  {{ .name }}: {{ .value | b64enc | quote }}
  {{- end }}
{{- end }}