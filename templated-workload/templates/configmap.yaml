{{- if .Values.app.environmentConfig }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
  {{- range .Values.app.environmentConfig }}
  {{ .name }}: {{ .value | quote }}
  {{- end }}
{{- end }}
